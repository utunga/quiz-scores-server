{
  "host": "quiz-scores-api.herokuapp.com/",
  "port": "PORT",
  "public": "../public/",
  "paginate": {
    "default": 10,
    "max": 50
  },
  // MONGO DB has been configured using Heroku Web UI 
  // see MONGODB_URI env varible - aka 
  // heroku config:get MONGODB_URI
}
